# FEATURES TO IMPLEMENT — Dogule1

Pending

- Fix Outlook mailto encoding: birthday email subject/body shows `+` instead of spaces in Outlook clients.
- Verify forced trainer mobile view renders visibly different; if not, revise CSS/layout until fonts and spacing are clearly larger on trainer login.
