name: Dependency Audit

on:
  schedule:
    - cron: '0 3 * * 1' # DLX-CI-AUDIT-CRON-001: wöchentlicher Audit, Mo 03:00 UTC
  workflow_dispatch:
  pull_request:
    branches: [ main ]

jobs:
  audit:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 10.19.0

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'pnpm'

      - run: pnpm install

      - name: Enable corepack
        run: corepack enable

      - name: Run pnpm audit
        run: |
          echo "DLX-CI-AUDIT-001: running pnpm audit in CI"
          pnpm audit
